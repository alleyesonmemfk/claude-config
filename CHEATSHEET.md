# Claude Code - Шпаргалка

## 🚀 Быстрый старт

```bash
# Установка в проект
./install.sh

# Запуск Claude Code
cd /путь/к/проекту
claude
```

## 📋 Команды

| Команда | Описание |
|---------|----------|
| `/verify` | Глубокий аудит кода с доказательной базой ошибок |
| `/build-and-fix` | Автономный запуск CI и исправление ошибок сборки |
| `/dev-docs` | Создать стратегический тех-план и декомпозицию |
| `/dev-docs-update` | Создать checkpoint перед сбросом контекста |
| `/test-auth-endpoint` | Тестирование защищённого JWT эндпоинта |

## 🤖 Agents

| Agent | Вызов | Применение |
|-------|-------|------------|
| auto-error-resolver | Автоматически | Исправление ошибок TypeScript |
| code-architecture-reviewer | Вручную | Архитектурный анализ |
| documentation-architect | Вручную | Создание документации |
| frontend-error-fixer | Автоматически | Исправление ошибок фронтенда |

## 🎯 Skills - Автоактивация

### Backend Development
```
Промпт: "Создай endpoint для users"
→ Активирует: fastapi-backend-guidelines
```

### Frontend Development
```
Промпт: "Добавь компонент UserCard"
→ Активирует: react-frontend-guidelines
```

### Database Migrations
```
Промпт: "Добавь колонку email"
→ Активирует: database-migrations
```

### OAuth Integration
```
Промпт: "Добавь Google OAuth"
→ Активирует: oauth-integration
```

### API Client Generation
```
Промпт: "Сгенерируй API клиент"
→ Активирует: api-client-generation
```

## 🛡️ Guardrails (Защита)

### Database Verification (БЛОКИРУЕТ)
```
❌ DROP TABLE users
❌ TRUNCATE data
❌ DELETE FROM без WHERE
→ Требует подтверждения
```

### Security Guardrail (БЛОКИРУЕТ)
```
❌ Хардкод паролей
❌ SQL injection уязвимости
❌ XSS уязвимости
→ Блокирует небезопасный код
```

### FSD Architecture Guard (ПРЕДУПРЕЖДАЕТ)
```
⚠️ Импорт pages в widgets
⚠️ Импорт features в entities
⚠️ Нарушение слоёв FSD
→ Показывает предупреждение
```

## 💡 Примеры промптов

### Backend
```bash
# Создание эндпоинта
"Создай POST /api/users endpoint с валидацией Pydantic"

# Сервисный слой
"Добавь UserService с методом create_user"

# Репозиторий
"Создай UserRepository с async методами"

# Миграция
"Создай миграцию для добавления role в users"
```

### Frontend
```bash
# Компонент
"Создай компонент UserCard в entities/User"

# Страница
"Добавь страницу UserProfile с Redux state"

# Виджет
"Создай виджет UserList с пагинацией"

# Хук
"Создай useUser hook с React Query"
```

### Database
```bash
# Создание таблицы
"Создай таблицу orders с связями"

# Изменение схемы
"Добавь индекс на email в users"

# Data migration
"Миграция для переноса данных из old_table"
```

### OAuth
```bash
# Google OAuth
"Добавь Google OAuth авторизацию"

# Telegram
"Интегрируй Telegram Login Widget"

# Account linking
"Реализуй привязку Google к существующему аккаунту"
```

## 🔧 Кастомизация

### Добавить skill
```bash
mkdir .claude/skills/my-skill
nano .claude/skills/my-skill/skill.md
```

Затем добавьте в `skills/skill-rules.json`:
```json
"my-skill": {
  "type": "domain",
  "enforcement": "suggest",
  "priority": "high",
  "promptTriggers": {
    "keywords": ["мой триггер"]
  }
}
```

### Добавить команду
```bash
nano .claude/commands/my-command.md
```

### Добавить агента
```bash
nano .claude/agents/my-agent.md
```

## 📊 Структура проекта

```
project/
├── .claude/
│   ├── settings.json          # Конфигурация
│   ├── agents/                # Агенты
│   ├── commands/              # Команды
│   ├── hooks/                 # Хуки
│   └── skills/                # Skills
│       └── skill-rules.json   # Правила активации
```

## 🐛 Troubleshooting

### Hook не работает
```bash
chmod +x .claude/hooks/skill-activation-prompt.sh
cat .claude/settings.json  # Проверить путь
```

### Skills не активируются
```bash
# Проверить валидность JSON
python3 -m json.tool .claude/skills/skill-rules.json

# Проверить логи (если есть)
tail -f ~/.claude/logs/hooks.log
```

### Зависимости отсутствуют
```bash
cd .claude/hooks
npm install @types/node
```

## 📚 Ключевые слова для активации

### Backend (русский)
```
backend, бэкенд, FastAPI, API, endpoint, эндпоинт,
роутер, router, сервис, service, репозиторий, repository,
SQLAlchemy, Pydantic, алембик, alembic, миграция, migration
```

### Frontend (русский)
```
компонент, component, react, UI, интерфейс, страница, page,
виджет, widget, фича, feature, Redux, React Query, хук, hook,
useState, useEffect, SCSS, стили, модуль
```

### Database (русский)
```
migration, миграция, alembic, алембик, schema, таблица, table,
column, колонка, database, база данных, db, DDL
```

### OAuth (русский)
```
OAuth, Google Login, Telegram Login, social auth,
аутентификация, привязка аккаунта, account linking,
авторизация, authorization
```

## 🎓 Best Practices

1. **Используйте описательные промпты**: "Создай FastAPI endpoint для получения списка пользователей с пагинацией"

2. **Указывайте слой архитектуры**: "Создай UserService в service layer"

3. **Упоминайте паттерны**: "Используй Repository Pattern для User"

4. **Указывайте путь**: "Создай компонент в widgets/UserProfile"

5. **Тестируйте guardrails**: Попробуйте "DROP TABLE" - должно заблокировать

## 🔗 Полезные ссылки

- [Полная документация](./.claude/README.md)
- [Инструкции по установке](./INSTALL.md)
- [Claude Code CLI](https://github.com/anthropics/claude-code)

## 📦 Что включено

- ✅ 4 специализированных агента
- ✅ 5 кастомных команд
- ✅ 15+ доменных skills
- ✅ 3 критических guardrails
- ✅ Автоматическая система активации
- ✅ Поддержка русского + английского

---

**Версия**: 1.0 | **Дата**: 2026-01-12
