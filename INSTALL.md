# Быстрая установка Claude Code конфигурации

## 🎯 Выберите вариант установки

### Вариант 1: Копирование в существующий проект

```bash
# Замените /path/to/your/project на путь к вашему проекту
cp -r "/Users/user/Downloads/конфиг-клод-кода/.claude" /path/to/your/project/

# Перейдите в проект
cd /path/to/your/project

# Запустите Claude Code
claude
```

### Вариант 2: Использование как отдельная рабочая директория

```bash
# Перейдите в эту папку
cd "/Users/user/Downloads/конфиг-клод-кода"

# Запустите Claude Code
claude
```

### Вариант 3: Глобальная конфигурация (для всех проектов)

```bash
# Скопируйте в домашнюю директорию
cp -r "/Users/user/Downloads/конфиг-клод-кода/.claude" ~/

# Теперь конфигурация будет применяться для всех проектов
# где нет локальной папки .claude
```

## ✅ Проверка установки

После установки запустите проверку:

```bash
# Проверьте структуру
ls -la .claude/

# Должны быть:
# - settings.json
# - agents/
# - commands/
# - hooks/
# - skills/

# Проверьте права на hooks
ls -la .claude/hooks/*.sh

# Все .sh файлы должны быть executable (-rwxr-xr-x)
```

## 🧪 Тестирование

### Тест 1: Проверка активации skills

Запустите Claude Code и напишите:

```
Создай новый FastAPI endpoint
```

Должен автоматически активироваться `fastapi-backend-guidelines` skill.

### Тест 2: Проверка команд

```
/verify
```

Должна запуститься команда глубокого аудита кода.

### Тест 3: Проверка guardrails

```
Удали все данные из таблицы users
```

Должен активироваться `database-verification` guardrail с блокировкой.

## 🔧 Если что-то не работает

### Hook не запускается

```bash
# Сделайте скрипт исполняемым
chmod +x .claude/hooks/skill-activation-prompt.sh

# Проверьте содержимое settings.json
cat .claude/settings.json
```

### Skills не активируются

```bash
# Проверьте skill-rules.json
cat .claude/skills/skill-rules.json

# Убедитесь, что файл валиден
cat .claude/skills/skill-rules.json | python3 -m json.tool
```

### Ошибки TypeScript в hooks

```bash
# Установите зависимости Node.js
cd .claude/hooks
npm install @types/node
```

## 📚 Дополнительная информация

Смотрите [.claude/README.md](./.claude/README.md) для полной документации.

## 🎨 Кастомизация под ваш проект

### Шаг 1: Отредактируйте пути в skill-rules.json

Откройте `.claude/skills/skill-rules.json` и измените пути под структуру вашего проекта:

```json
"fileTriggers": {
  "pathPatterns": [
    "back/app/features/**/*.py",  // Замените на ваши пути
    "front/src/pages/**/*.tsx"
  ]
}
```

### Шаг 2: Добавьте свои ключевые слова

Добавьте специфичные для вашего проекта ключевые слова:

```json
"promptTriggers": {
  "keywords": [
    "ваш-специфичный-термин",
    "название-вашего-модуля"
  ]
}
```

### Шаг 3: Создайте свои skills

```bash
# Создайте новый skill
mkdir .claude/skills/my-project-skill
nano .claude/skills/my-project-skill/skill.md
```

## 🚀 Готово!

Теперь у вас есть полноценная конфигурация Claude Code с:
- ✅ 4 специализированными агентами
- ✅ 5 кастомными командами
- ✅ 15+ доменными skills
- ✅ Автоматической системой активации
- ✅ Guardrails для критических операций

Запустите `claude` в вашем проекте и начните работу!
