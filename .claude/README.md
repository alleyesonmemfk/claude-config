# Claude Code Configuration

Эта папка содержит полную конфигурацию Claude Code для интеграции в ваши проекты.

## 📁 Структура

```
.claude/
├── settings.json              # Основные настройки и хуки
├── agents/                    # Специализированные агенты
│   ├── auto-error-resolver.md
│   ├── code-architecture-reviewer.md
│   ├── documentation-architect.md
│   └── frontend-error-fixer.md
├── commands/                  # Кастомные команды
│   ├── build-and-fix.md
│   ├── dev-docs.md
│   ├── dev-docs-update.md
│   ├── test-auth-endpoint.md
│   └── verify.md
├── hooks/                     # Автоматические хуки
│   ├── skill-activation-prompt.sh
│   ├── skill-activation-prompt.ts
│   └── node_modules/          # Зависимости TypeScript
└── skills/                    # Доменные знания
    ├── skill-rules.json       # Правила активации скиллов
    ├── fastapi-backend-guidelines/
    ├── react-frontend-guidelines/
    ├── database-migrations/
    ├── oauth-integration/
    ├── api-client-generation/
    └── skill-developer/
```

## 🚀 Использование

### Вариант 1: Интеграция в существующий проект

Скопируйте всю папку `.claude` в корень вашего проекта:

```bash
cp -r .claude /путь/к/вашему/проекту/
```

### Вариант 2: Глобальная конфигурация

Используйте эту папку как рабочую директорию для Claude Code:

```bash
cd "/Users/user/Downloads/конфиг-клод-кода"
claude
```

## 🎯 Что умеет эта конфигурация

### 1. Автоматическая активация skills

Skills активируются автоматически на основе:
- **Ключевых слов** в промптах (русский + английский)
- **Паттернов намерений** (regex)
- **Упомянутых файлов** в контексте
- **Содержимого файлов**

Примеры:
- "Создай новый endpoint" → активирует `fastapi-backend-guidelines`
- "Добавь компонент React" → активирует `react-frontend-guidelines`
- Работа с файлом `back/alembic/versions/*.py` → активирует `database-migrations`

### 2. Agents (Специализированные агенты)

#### `auto-error-resolver`
Автоматическое исправление ошибок компиляции TypeScript.

#### `code-architecture-reviewer`
Глубокий архитектурный анализ кода.

#### `documentation-architect`
Создание и обновление документации.

#### `frontend-error-fixer`
Исправление ошибок фронтенда.

### 3. Commands (Кастомные команды)

#### `/build-and-fix`
Автономный запуск CI-пайплайна и исправление ошибок сборки.

#### `/dev-docs`
Создать стратегический тех-план, декомпозицию задач и структуру файлов.

#### `/dev-docs-update`
Создать контрольную точку (Checkpoint) перед сбросом контекста.

#### `/test-auth-endpoint`
Тестирование защищённого эндпоинта с JWT токеном.

#### `/verify`
Глубокий аудит кода с доказательной базой ошибок.

### 4. Hooks (Автоматические триггеры)

**UserPromptSubmit Hook** (`skill-activation-prompt.sh`)
- Анализирует каждый промпт перед отправкой
- Автоматически предлагает релевантные skills
- Работает на основе правил из `skills/skill-rules.json`

### 5. Skills (Доменные знания)

#### Guardrails (критические, блокирующие)
- **database-verification**: Предотвращает потерю данных при миграциях
- **security-guardrail**: Проверка на хардкод секретов, SQL injection, XSS
- **fsd-architecture-guard**: Контроль соблюдения FSD архитектуры

#### Domain Skills (рекомендательные)
- **fastapi-backend-guidelines**: FastAPI/Python/SQLAlchemy паттерны
- **react-frontend-guidelines**: React/TypeScript/FSD практики
- **database-migrations**: Alembic миграции
- **oauth-integration**: OAuth 2.0 Google/Telegram
- **api-client-generation**: Orval генерация клиентов
- **skill-developer**: Мета-скилл для создания новых skills

## ⚙️ Настройка

### Файл `settings.json`

```json
{
  "hooks": {
    "UserPromptSubmit": ".claude/hooks/skill-activation-prompt.sh"
  }
}
```

### Файл `skills/skill-rules.json`

Конфигурация триггеров для автоматической активации skills. Основные параметры:

- **enforcement**: `block` (блокирует), `warn` (предупреждает), `suggest` (предлагает)
- **priority**: `critical`, `high`, `medium`, `low`
- **promptTriggers**: Ключевые слова и regex паттерны
- **fileTriggers**: Паттерны путей и содержимого файлов

## 🛠️ Кастомизация

### Добавление нового skill

1. Создайте папку в `.claude/skills/`:
```bash
mkdir -p .claude/skills/my-new-skill
```

2. Создайте файл `skill.md` с промптом:
```markdown
---
name: my-new-skill
description: Описание моего skill
version: 1.0
---

# My New Skill

[Ваши инструкции для Claude]
```

3. Добавьте правила активации в `skills/skill-rules.json`:
```json
"my-new-skill": {
  "type": "domain",
  "enforcement": "suggest",
  "priority": "high",
  "description": "Краткое описание",
  "promptTriggers": {
    "keywords": ["ключевое слово"],
    "intentPatterns": ["regex.*pattern"]
  }
}
```

### Добавление нового агента

Создайте `.claude/agents/my-agent.md`:
```markdown
---
name: my-agent
description: Описание агента
---

# My Agent

[Инструкции для агента]
```

### Добавление новой команды

Создайте `.claude/commands/my-command.md`:
```markdown
---
name: my-command
description: Описание команды
---

# My Command

[Инструкции для команды]
```

## 📊 Статистика конфигурации

- **Agents**: 4
- **Commands**: 5
- **Skills**: 15+
- **Hooks**: 1 активный (UserPromptSubmit)
- **Поддержка языков**: Русский + English

## 🔗 Интеграция с проектами

Эта конфигурация оптимизирована для:
- **Backend**: FastAPI, Python, SQLAlchemy, Alembic
- **Frontend**: React, TypeScript, Redux Toolkit, React Query v5, FSD
- **Database**: PostgreSQL, миграции Alembic
- **Auth**: OAuth 2.0, JWT, Google, Telegram
- **ML**: BERTopic, кластеризация интервью

## 📝 Примеры использования

### Пример 1: Создание бэкенд эндпоинта
```
Промпт: "Создай endpoint для получения списка пользователей"
→ Автоматически активируется fastapi-backend-guidelines
→ Claude применит паттерны: Router → Service → Repository → DTO
```

### Пример 2: Создание React компонента
```
Промпт: "Добавь виджет UserProfile с Redux"
→ Автоматически активируется react-frontend-guidelines
→ Claude применит FSD структуру и Redux Toolkit паттерны
```

### Пример 3: Миграция базы данных
```
Промпт: "Добавь колонку email в таблицу users"
→ Автоматически активируется database-migrations
→ Claude создаст Alembic миграцию с upgrade/downgrade
```

### Пример 4: Критическая операция
```
Промпт: "DROP TABLE users"
→ Активируется database-verification (enforcement: block)
→ Claude БЛОКИРУЕТ операцию и требует подтверждения
```

## 🎓 Дополнительная информация

### Как работает hook система

1. **UserPromptSubmit** запускается перед каждым промптом
2. Hook анализирует текст промпта + контекст файлов
3. Сравнивает с правилами в `skill-rules.json`
4. Если есть совпадение → добавляет skill в контекст
5. Claude получает расширенный промпт со skill инструкциями

### Типы enforcement

- **block**: Останавливает выполнение, требует явного подтверждения
- **warn**: Показывает предупреждение, но разрешает продолжить
- **suggest**: Мягко предлагает использовать skill

### Priority levels

- **critical**: Всегда активируется при совпадении
- **high**: Активируется для большинства совпадений
- **medium**: Активируется для явных совпадений
- **low**: Активируется только при явном упоминании

## 🚨 Troubleshooting

### Hook не работает
```bash
# Проверьте права
chmod +x .claude/hooks/skill-activation-prompt.sh

# Проверьте settings.json
cat .claude/settings.json
```

### Skills не активируются
```bash
# Проверьте skill-rules.json
cat .claude/skills/skill-rules.json | jq

# Проверьте логи hook (если есть)
tail -f ~/.claude/logs/hooks.log
```

### Node.js зависимости отсутствуют
```bash
cd .claude/hooks
npm install @types/node
```

## 📧 Поддержка

Эта конфигурация создана для работы с Claude Code CLI.
Документация: https://github.com/anthropics/claude-code

---

**Версия конфигурации**: 1.0
**Последнее обновление**: 2026-01-12
**Совместимость**: Claude Code 1.x
