# Анализ совместимости конфигурации Claude Code с Cursor

## 📋 Краткое резюме

**Вывод:** Конфигурация Claude Code **НЕ совместима напрямую** с Cursor, но **большую часть контента можно адаптировать**.

---

## 🔍 Структура конфигурации Claude Code

### 1. **Agents** (`agents/`)
**Назначение:** Специализированные агенты для конкретных задач
- `auto-error-resolver.md` - Автоматическое исправление ошибок TypeScript
- `code-architecture-reviewer.md` - Код-ревью и архитектурная проверка
- `documentation-architect.md` - Генерация документации
- `frontend-error-fixer.md` - Исправление фронтенд ошибок

**Формат:** Markdown файлы с frontmatter (YAML заголовком)
- Имя агента
- Описание
- Модель (например, `sonnet`)
- Инструкции для AI

### 2. **Commands** (`commands/`)
**Назначение:** Автономные команды для выполнения сложных задач
- `build-and-fix.md` - CI/CD пайплайн и исправление ошибок сборки
- `dev-docs.md` - Создание технической документации
- `verify.md` - Верификация кода

**Формат:** Markdown с описанием процесса и шагов

### 3. **Hooks** (`hooks/`)
**Назначение:** Автоматические скрипты, выполняемые на разных этапах
- `skill-activation-prompt.ts` - Активация скиллов на основе промпта
- `pre-tool-use-guardrails.ts` - Защита от опасных паттернов кода
- Различные shell-скрипты для валидации

**Технологии:** TypeScript/Node.js, Bash
**Механизм:** Хуки интегрируются через `.claude/settings.json`

### 4. **Skills** (`skills/`)
**Назначение:** Доменные знания и правила активации
- `skill-rules.json` - Конфигурация триггеров активации
- Папки со скиллами (FastAPI, React, FSD и т.д.)
- Документация и примеры

**Механизм активации:**
- Ключевые слова в промпте
- Паттерны намерений (regex)
- Паттерны путей файлов (glob)
- Паттерны содержимого файлов (regex)

---

## ⚠️ Что НЕ работает в Cursor

### 1. **Система хуков (Hooks)**
```
❌ UserPromptSubmit hook - специфичен для Claude Code
❌ PreToolUse hook - специфичен для Claude Code
❌ Skill activation system - требует интеграции с Cursor
❌ Guardrails system - нет встроенной поддержки
```

**Причина:** Cursor не имеет системы хуков, аналогичной Claude Code. Хуки - это интеграционная точка Claude Code с его runtime.

### 2. **Автоматическая активация скиллов**
```
❌ skill-rules.json с триггерами
❌ Автоматическое предложение скиллов
❌ Блокирующие guardrails
❌ Отслеживание сессий
```

**Причина:** Cursor использует другую архитектуру взаимодействия с AI.

### 3. **Формат агентов**
```
❌ YAML frontmatter с полями name, description, model, color
❌ Автоматический запуск агентов через Task инструмент
```

**Причина:** Разные форматы конфигурации.

---

## ✅ Что МОЖНО адаптировать для Cursor

### 1. **Документация Skills → `.cursorrules` или контекст**

**Вариант А: Создать `.cursorrules`**
```markdown
# .cursorrules

## Backend Development (FastAPI)
[Содержимое из skills/fastapi-backend-guidelines/SKILL.md]

## Frontend Development (React/FSD)
[Содержимое из skills/react-frontend-guidelines/SKILL.md]

## Database Migrations
[Содержимое из skills/database-migrations/SKILL.md]
```

**Вариант Б: Использовать как справочную документацию**
- Хранить в папке `docs/` или `.cursor/`
- Ссылаться в промптах при необходимости

### 2. **Агенты → Шаблоны промптов**

Можно создать файлы-шаблоны для копирования в чат:
```
.cursor/prompts/
  ├── code-review.md
  ├── error-fix.md
  └── documentation.md
```

Или использовать как инструкции в системных правилах.

### 3. **Commands → Процессы и инструкции**

Адаптировать команды как:
- Документация в `README.md` или `CONTRIBUTING.md`
- Чек-листы для ручного выполнения
- GitHub Actions workflows (для автоматизации CI/CD)

### 4. **Guardrails → Линтеры и pre-commit hooks**

Вместо TypeScript хуков использовать:
- ESLint rules (для TypeScript)
- Prettier configuration
- Pre-commit hooks (husky + lint-staged)
- TypeScript strict mode

---

## 🛠️ Рекомендации по адаптации

### Шаг 1: Извлечь полезную документацию

Создать структуру:
```
.cursor/
  ├── rules/
  │   ├── backend.md          # Из fastapi-backend-guidelines
  │   ├── frontend.md         # Из react-frontend-guidelines
  │   ├── database.md         # Из database-migrations
  │   └── architecture.md     # Из skill-developer
  └── prompts/
      ├── code-review.md      # Из code-architecture-reviewer
      └── error-fix.md        # Из auto-error-resolver
```

### Шаг 2: Создать `.cursorrules`

Объединить ключевые правила из skills в один файл `.cursorrules` в корне проекта.

### Шаг 3: Адаптировать guardrails

Вместо хуков использовать:
```json
// .eslintrc.json
{
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "no-console": ["warn", { "allow": ["warn", "error"] }]
  }
}
```

### Шаг 4: Команды → GitHub Actions / npm scripts

```json
// package.json
{
  "scripts": {
    "build-and-fix": "npm run type-check && npm run lint:fix",
    "verify": "npm run test && npm run type-check"
  }
}
```

---

## 📊 Сравнительная таблица

| Компонент Claude Code | Статус в Cursor | Альтернатива |
|----------------------|-----------------|--------------|
| **Hooks (TypeScript)** | ❌ Не работает | ESLint, Pre-commit hooks |
| **Skills (автоактивация)** | ❌ Не работает | `.cursorrules`, контекст |
| **Agents (формат)** | ⚠️ Частично | Шаблоны промптов |
| **Commands** | ✅ Можно адаптировать | npm scripts, GitHub Actions |
| **Документация Skills** | ✅ Полностью применима | `.cursorrules`, docs/ |
| **Guardrails логика** | ⚠️ Частично | Линтеры, TypeScript strict |

---

## 🎯 Практический план миграции

### Фаза 1: Быстрый старт (1-2 часа)
1. Скопировать содержимое ключевых skills в `.cursorrules`
2. Создать `.cursor/prompts/` с шаблонами из agents
3. Добавить основные правила в `.eslintrc` (из guardrails)

### Фаза 2: Полная адаптация (1 день)
1. Организовать документацию по структуре
2. Настроить pre-commit hooks для базовых проверок
3. Создать npm scripts для команд
4. Настроить TypeScript strict mode

### Фаза 3: Оптимизация (опционально)
1. Настроить GitHub Actions для CI/CD
2. Создать детальные шаблоны промптов
3. Документировать процессы команды

---

## 💡 Выводы

**Позитивные моменты:**
- ✅ Вся документация и знания применимы
- ✅ Логика guardrails может быть реализована через линтеры
- ✅ Команды можно автоматизировать
- ✅ Агенты становятся полезными шаблонами

**Ограничения:**
- ❌ Нет автоматической активации скиллов
- ❌ Нет системы хуков для блокировок
- ❌ Требуется ручная адаптация контента

**Рекомендация:** 
Конфигурация очень ценная как источник знаний и практик. Адаптировать её стоит, но нужно понимать, что автоматизация будет работать иначе (через линтеры и CI/CD, а не через хуки).
